#include "ObjectManager.h"

ObjectMenager::ObjectMenager(){

}

ObjectMenager::~ObjectMenager(){

}
/*
void ObjectMenager::AddToMenager(GameObject object) {
	objects.push_back(&object);
}

//void ObjectMenager::DeleteFromMenager(GameObject* object) {
	//objects.erase(std::remove(objects.begin(), objects.end(), object), objects.end()); //Erase-remove idiom
//}

void ObjectMenager::UpdateObjects() {
	for (int i = 0; i < objects.size(); i++)
	{
		objects[i]->Update();
	}*/

	/*for (GameObject* const& ii : objects) {
		ii->Update();
	}
}

/*void ObjectMenager::RenderObjects() {
	for (int i = 0; i < objects.size(); i++)
	{
		objects[i]->Render();
	}
}*/